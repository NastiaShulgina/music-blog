import { StyledSelections } from "./styles/Selections.styled";

const Selections = () => {
    return (
        <StyledSelections>
            <h4>Your selections:</h4>
            <hr />
            <p>No selections yet.</p>
        </StyledSelections>
    );
}

export default Selections;